// console.log(2 > 1); 
// console.log(2 >= 2);
// console.log(2 < 1);
// console.log(2 == 1);
// console.log(2 != 1);

console.log("2" > 1);// avoid comparision this code only clean code prority only clean code
console.log("02" > 1);// avoid comparision this code only clean code prority only clean code

console.log(null > 0);// avoid comparision this code only clean code prority only clean code
console.log(null == 0);// avoid comparision this code only clean code prority only clean code
console.log(null >= 0)// avoid comparision this code only clean code prority only clean code
console.log(undefined == 0)
// avoid comparision this code only clean code prority only clean code
// Comparision and equlity check is differnt methods 

// === strict check 

console.log("2" === 2); 







